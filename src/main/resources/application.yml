spring:
  h2:
    console:
      settings:
        web-allow-others: true
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop

hibernate:
  dialect:  org.hibernate.dialect.H2Dialect

